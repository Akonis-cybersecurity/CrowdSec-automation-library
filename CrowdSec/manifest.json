{
  "configuration": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "CrowdSec",
    "type": "object",
    "properties": {
      "base_url": {
        "description": "CrowdSec LAPI base URL (e.g. http://127.0.0.1:8080)",
        "type": "string"
      },
      "api_key": {
        "description": "Bouncer API key (required for the decision trigger and GetDecisions action)",
        "type": "string"
      },
      "machine_id": {
        "description": "Watcher machine ID (required for the alert trigger and all write/read actions)",
        "type": "string"
      },
      "password": {
        "description": "Watcher password (required for the alert trigger and all write/read actions)",
        "type": "string"
      }
    },
    "required": ["base_url"],
    "secrets": ["api_key", "password"]
  },
  "description": "CrowdSec is an open-source collaborative IPS. This module connects to the CrowdSec Local API (LAPI) to ingest alerts and decisions, and to perform remediation actions.",
  "name": "CrowdSec",
  "slug": "crowdsec",
  "uuid": "b3eb6b25-6093-4129-bd2b-c521e464db59",
  "version": "1.0.0",
  "categories": ["Network", "Threat Intelligence"]
}
